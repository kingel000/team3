<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="main.project.web.product.dao.IProductDAO">
	<insert id="insertProduct" parameterType="ProductVO">
		INSERT INTO product VALUES
		(#{product_num},#{category},#{product_title},#{expert_id},
		 #{expert_info},#{product_file},#{product_date})
	</insert>
	<update id="updateProduct" parameterType="ProductVO">
		UPDATE product SET
		category=#{category},product_title=#{product_title},product_info=#{expert_info},
		product_file=#{product_file},product_date=#{product_date}
		WHERE product_num=#{product_num}
	</update>
	<delete id="deleteProduct" parameterType="ProductVO">
		DELETE FROM product
		WHERE product_num=#{product_num}
	</delete>
	<select id="selectProduct" parameterType="String" resultType="ProductVO">
		SELECT * FROM product
		WHERE product_num=#{product_num}
	</select>
	<select id="selectListProduct" resultType="ProductVO">
		SELECT * FROM product
		WHERE expert_id=#{id}
	</select>
	
</mapper>
