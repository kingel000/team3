<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="main.project.admin.board.dao.adminIBoardNoticeDAO">

	<insert id="insertBoardNotice" parameterType="abnVO">
		<!-- 
			insert into board_notice(

			board_notice_title,
			board_notice_info,

		)
		values
		(

			#{board_notice_title},
			#{board_notice_info},

		)	
		 -->
	
		insert into board_notice(
			board_notice_num,
			board_notice_title,
			board_notice_info,
			board_notice_date
		)
		values
		(
			#{board_notice_num},
			#{board_notice_title},
			#{board_notice_info},
			sysdate
		)	
	</insert>

	<select id="selectListAdminBoardNotice" resultType="abnVO">
		SELECT * FROM board_notice order by board_notice_num desc
	</select>
	
</mapper>
